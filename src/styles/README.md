Tailwind utility classes cover almost all styling (see tailwind.config.js for
design tokens). Global overrides live in src/index.css. Add component-scoped
CSS Modules here only if a future component genuinely needs styles Tailwind
can't express.
