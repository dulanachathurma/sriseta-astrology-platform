This folder is available for any imported (bundled) image assets, e.g.
`import logo from './assets/logo.png'`. For the splash-screen logo and
favicon, drop files directly into /public instead (see /public/images/README.md) —
that lets you swap them without touching code.
